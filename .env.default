# VHF Listening Station Configuration Template
# Copy to .env and fill in your actual values

# Icecast Server Settings (used by Pi FFmpeg service to connect)
ICECAST_HOST=your-aws-host.amazonaws.com
ICECAST_PORT=8888
ICECAST_PASSWORD=your-secure-password-here
ICECAST_MOUNT=/vhf_stream.mp3

# Icecast Server Configuration (used by AWS setup script only)
ICECAST_ADMIN_PASSWORD=your-admin-password-here

# SSH Key for AWS setup script
SSH_KEY=~/.ssh/your-aws-key.pem

# Pi Deployment Settings (used by deploy_to_pi.sh)
PI_HOST=pi@192.168.0.200
PROJECT_DIR=vhf-listening